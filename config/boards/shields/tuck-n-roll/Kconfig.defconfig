if SHIELD_TUCK-N-ROLL_LEFT

config ZMK_KEYBOARD_NAME
	default "Tuck-n-roll"

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_TUCK-N-ROLL_LEFT || SHIELD_TUCK-N-ROLL_RIGHT

config ZMK_SPLIT
	default y

endif
